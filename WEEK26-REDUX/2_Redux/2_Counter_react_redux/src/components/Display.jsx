import { useSelector } from "react-redux";

const Display = () => {
  // TO GET COUNTER STATE FROM STORE IN REDUX  :
  // useSelector();
  return <p className="reduc-counter-display">200</p>;
};
export default Display;
